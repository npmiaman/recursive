{
  "name": "recursive",
  "version": "0.1.0",
  "type": "module",
  "description": "Software that finds its own breakage, contains it, and opens a verified fix. Install it into a project and it learns the codebase, sweeps it in a real browser, and repairs what breaks.",
  "bin": {
    "recursive": "bin/recursive.mjs"
  },
  "files": [
    "bin",
    "src",
    "packages",
    "fixtures",
    "tsconfig.json",
    ".env.example",
    "README.md"
  ],
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cli": "node --experimental-strip-types --no-warnings src/cli.ts",
    "snapshot": "npm run cli -- snapshot",
    "diagnose": "npm run cli -- diagnose",
    "score": "npm run cli -- score",
    "fix": "npm run cli -- fix",
    "verify": "npm run cli -- verify",
    "test": "node --experimental-strip-types --no-warnings test/scrub.test.ts && node --experimental-strip-types --no-warnings test/retrieve.test.ts && node --experimental-strip-types --no-warnings test/memory.test.ts && node --experimental-strip-types --no-warnings test/cohort.test.ts && node --experimental-strip-types --no-warnings test/ratelimit.test.ts && node --experimental-strip-types --no-warnings test/provider.test.ts",
    "test:browse": "node --experimental-strip-types --no-warnings test/browse.test.ts",
    "eval:retrieve": "node --experimental-strip-types --no-warnings test/retrieve-eval.ts",
    "test:loop": "node --experimental-strip-types --no-warnings test/closed-loop.test.ts",
    "test:all": "npm run test && npm run test:loop && npm run test:backend",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\" \"apps/web/**/*.{ts,tsx}\"",
    "test:backend": "node --experimental-strip-types --no-warnings test/backend-verify.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@anthropic-ai/sdk": "^0.113.0",
    "playwright": "^1.49.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
